Title
========================================================



```{r dismo, results='hide'}
library(dismo)
OR_map <- gmap("United States")
plot(OR_map, interpolate = TRUE, main = "Map of Oregon")

```